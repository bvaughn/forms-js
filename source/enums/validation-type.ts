module formsjs {

  /**
   * Supported type validations.
   */
  export enum ValidationType {
    ARRAY = <any>Array,
    BOOLEAN = <any>"boolean",
    FLOAT = <any>"float",
    INTEGER = <any>"integer",
    STRING = <any>"string"
  }
}